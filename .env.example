#########################################
# Denser — Example environment variables
#+ Replace placeholder values with your own credentials.
#+ Do NOT commit a real `.env` file to source control.
#########################################

# Firebase web app configuration (values from Firebase console)
FIREBASE_API_KEY=
FIREBASE_AUTH_DOMAIN=
FIREBASE_PROJECT_ID=
FIREBASE_STORAGE_BUCKET=
FIREBASE_MESSAGING_SENDER_ID=
FIREBASE_APP_ID=

# API key for the generative AI provider used (Gemini, OpenAI, etc.)
GEMINI_API_KEY=

# Optional: Sentry DSN or other third-party keys
SENTRY_DSN=

#########################################
# Usage notes
#########################################
# 1) Save your real secrets in a file named `.env` (not tracked by git).
# 2) Load env vars in Node.js (if using dotenv):
#    - Install: `npm install dotenv`
#    - In your entry file (e.g., `src/main.js`):
#      require('dotenv').config();
#      // then access with process.env.FIREBASE_API_KEY
#
# 3) Example Firebase init (JavaScript):
#    const firebaseConfig = {
#      apiKey: process.env.FIREBASE_API_KEY,
#      authDomain: process.env.FIREBASE_AUTH_DOMAIN,
#      projectId: process.env.FIREBASE_PROJECT_ID,
#      storageBucket: process.env.FIREBASE_STORAGE_BUCKET,
#      messagingSenderId: process.env.FIREBASE_MESSAGING_SENDER_ID,
#      appId: process.env.FIREBASE_APP_ID
#    };

# 4) PowerShell quick tips (Windows):
#    - To run a local server and export an env var for the current session:
#      $env:GEMINI_API_KEY = 'sk-xxxx'; python -m http.server 5173
#    - To permanently set user environment variables (PowerShell):
#      [Environment]::SetEnvironmentVariable('GEMINI_API_KEY','sk-xxxx','User')
#
# 5) Local dev helpers:
#    - Use `env-cmd`, `cross-env`, or `.env.local` files for per-environment configs.
#    - If using Vite, Next.js, or Create React App, follow their env var naming conventions
#      (e.g., `VITE_` or `NEXT_PUBLIC_` prefixes for variables exposed to client-side code).

# 6) Example (commented) values — remove comments before use
# FIREBASE_API_KEY=abc123
# GEMINI_API_KEY=sk-xxxx

